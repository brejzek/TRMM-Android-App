# TacticalRMM Mobile App Configuration

# Default API Path Segments (Comma separated if multiple)
VITE_API_PATH_SEGMENTS=/api/v3/agents/,/agents/

# Default MeshCentral Fallback Host (Optional)
# If not set, the app will attempt to derive it from the API URL
VITE_MESH_CENTRAL_FALLBACK=
